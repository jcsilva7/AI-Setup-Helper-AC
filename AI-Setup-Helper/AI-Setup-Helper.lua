local car

-- UI functions
local UI = require("ui")

function script.onSessionStart(sessionIndex)

end

function script.update(dt)
    
end

-- Main part
function script.windowMain(dt)
    
end

-- Settings UI
function script.windowSettings()
    -- TODO: change thresholds
end